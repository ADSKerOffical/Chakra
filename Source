local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Budgie Hub | Superpower simulator: Chakra",
    SubTitle = "by ADSKer",
    TabWidth = 160,
    Size = UDim2.new(0.7, 0, 1, 0),
    Acrylic = false,
    Theme = "Darker",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "" })
}

Tabs.Main:AddToggle("Defense", {
  Title = "Auto train",
  Description = "You pump up strength, defense and agility at the same time",
  Default = false,
  Callback = function(Value)
     turr = Value
 while turr and task.wait(0.15) do
   local ab = {"Strength", "Defense", "Agility"}
      for _, tex in ipairs(ab) do
        game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("Training"):FireServer(tex)
      end
    end
  end
})

Tabs.Main:AddToggle("a", {
 Title = "Auto buy Strength",
 Default = false,
 Description = "You automatically buy a strength multiplier",
 Callback = function(Value)
kika = Value
  while kika and task.wait(0.05) do
     game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("UpgradeMultiplier"):FireServer("Strength")
   end
 end    
})

Tabs.Main:AddToggle("a", {
 Title = "Auto buy Defense",
 Description = "You automatically buy a defense multiplier",
 Default = false,
 Callback = function(Value)
fufu = Value
  while fufu and task.wait(0.05) do
    game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("UpgradeMultiplier"):FireServer("Defense")
  end
 end    
})

Tabs.Main:AddToggle("a", {
 Title = "Auto buy Ability",
 Description = "You automatically buy a agility multiplier",
 Default = false,
 Callback = function(Value)
fuck = Value
   while fuck and task.wait(0.05) do
     game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("UpgradeMultiplier"):FireServer("Agility")
   end
 end    
})

Tabs.Main:AddButton({
 Title = "Redeem codes",
 Description = "You activate all codes (SUPER, RELEASE, 2XCOINS)",
 Callback = function()
    for _, code in next, {"SUPER", "RELEASE", "2XCOINS"} do
      game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("UseCode"):InvokeServer(code)
    end
 end
})

Tabs.Main:AddButton({
    Title = "Collect all quartz",
    Description = "Collects all the quartz on the entire map",
    Callback = function()
        local pos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
for _, col in next, Workspace.QuartzCollectibles:GetDescendants() do
   if col:IsA("ProximityPrompt") then
      game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = col.Parent.CFrame
      repeat task.wait() fireproximityprompt(col, 0) until col.Parent == nil
   end
end
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = pos
    end
})

Tabs.Main:AddButton({
 Title = "Anti afk",
 Description = "You won't get kicked after 20 minutes of AFK",
 Callback = function()
 for i,v in pairs(getconnections(game.Players.LocalPlayer.Idled)) do
  v:Disable()
 end
   end    
})

local shards = {"Red", "Orange", "Green", "Blue", "Purple", "Silver", "Pink"}
Tabs.Main:AddButton({
 Title = "Complete the quest by Stan Lee",
 Description = "Automatically completes Stan Lee\'s quest",
 Callback = function()
game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("QuestGiverInteract"):FireServer("Stan Lee")

for _, name in ipairs(shards) do
local args = {
    [1] = workspace:WaitForChild("ScavengerPieces"):WaitForChild(name .. " Shard")
}

game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("ScavengerHunt"):FireServer(unpack(args))
end

game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("QuestGiverInteract"):FireServer("Stan Lee")
   end    
})

Tabs.Main:AddToggle("A", {
 Title = "Auto farm brawlers",
 Default = false,
 Callback = function(Value)
tatat = Value
  while tatat do
 for _, person in ipairs(workspace:GetDescendants()) do
    pcall(function()
      if tatat == false then return end
  if person:IsA("Humanoid") and person.Parent.Name == "Brawler" and person.Health > 0 then
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = person.RootPart.CFrame * CFrame.new(0, 0, 1)
game:GetService("RunService").RenderStepped:Wait()
game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("UseSkill"):FireServer("Punch", false, false, person)
task.wait(0.4)
   end
  end)
 end
 task.wait(0.25)
end
 end    
})

Tabs.Main:AddToggle("A", {
 Title = "Auto farm goons",
 Default = false,
 Callback = function(Value)
tbtbt = Value
   while tbtbt do
 for _, person in ipairs(workspace:GetDescendants()) do
    pcall(function()
      if tbtbt == false then return end
  if person:IsA("Humanoid") and person.Parent.Name == "Goon" and person.Health > 0 then
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = person.RootPart.CFrame * CFrame.new(0, 0, 1)
game:GetService("RunService").RenderStepped:Wait()
game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("UseSkill"):FireServer("Punch", false, false, person)
task.wait(0.4)
   end
  end)
 end
 task.wait(0.25)
end
 end    
})

Tabs.Main:AddToggle("A", {
 Title = "Auto farm crooks",
 Default = false,
 Callback = function(Value)
tctct = Value
  while tctct do
 for _, person in ipairs(workspace:GetDescendants()) do
    pcall(function()
      if tctct == false then return end
  if person:IsA("Humanoid") and person.Parent.Name == "Crook" and person.Health > 0 then
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = person.RootPart.CFrame * CFrame.new(0, 0, 1)
game:GetService("RunService").RenderStepped:Wait()
game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("UseSkill"):FireServer("Punch", false, false, person)
task.wait(0.4)
   end
  end)
 end
 task.wait(0.25)
end
 end    
})

Tabs.Main:AddToggle("A", {
 Title = "Auto farm purgers",
 Default = false,
 Callback = function(Value)
tdtdt = Value
  while tdtdt do
 for _, person in ipairs(workspace:GetDescendants()) do
    pcall(function()
      if tdtdt == false then return end
  if person:IsA("Humanoid") and person.Parent.Name == "Purger" and person.Health > 0 then
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = person.RootPart.CFrame * CFrame.new(0, 0, 1)
game:GetService("RunService").RenderStepped:Wait()
game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("UseSkill"):FireServer("Punch", false, false, person)
task.wait(0.4)
   end
  end)
 end
 task.wait(0.25)
end
 end    
})

Tabs.Main:AddToggle("A", {
 Title = "Auto farm brutes",
 Default = false,
 Callback = function(Value)
tetet = Value
  while tetet do
 for _, person in ipairs(workspace:GetDescendants()) do
    pcall(function()
      if tetet == false then return end
  if person:IsA("Humanoid") and person.Parent.Name == "Brute" and person.Health > 0 then
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = person.RootPart.CFrame * CFrame.new(0, 0, 1)
game:GetService("RunService").RenderStepped:Wait()
game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("UseSkill"):FireServer("Punch", false, false, person)
task.wait(0.4)
   end
  end)
 end
 task.wait(0.25)
end
 end    
})
